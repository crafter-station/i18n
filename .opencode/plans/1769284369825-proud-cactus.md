# Plan: Code Cleanup and Latency Optimization

## Current State Review

The code is functional. Key optimizations for minimal latency:

### Already Optimized:
- **TTS**: Using `eleven_flash_v2_5` with `optimizeStreamingLatency: 4` and `speed: 1.1`
- **Translation**: Using Groq's `llama-3.1-8b-instant` (fastest model)
- **Transcription**: Using Deepgram's `nova-2` model

### Cleanup Needed:
1. Remove excessive console.log statements (reduce noise, slight perf gain)
2. Remove unused `username` parameter in `useTranscription` (not used anymore)
3. Remove unused `roomId` parameter in `CallUI`
4. Run linter to catch any issues

### Latency Optimizations:
1. **Start TTS generation while still processing** - Don't wait for state updates before calling TTS
2. **Remove unnecessary timeouts** - `setTimeout(() => setLiveTranscript(null), 1500)` delays next update

---

## Files to Modify

| File | Changes |
|------|---------|
| `src/components/video-call/hooks/use-transcription.ts` | Remove verbose logs, remove unused params, optimize flow |
| `src/components/video-call/hooks/use-tts.ts` | Remove verbose logs |
| `src/app/api/translate/route.ts` | Keep minimal logs |
| `src/app/api/tts/route.ts` | Already clean |
| `src/components/video-call/call-ui.tsx` | Remove unused roomId param, clean logs |

---

## Implementation

### 1. Clean use-transcription.ts
- Remove `username` param (not used since we skip own transcriptions)
- Reduce logging to essential errors only
- Remove debug console.logs

### 2. Clean use-tts.ts
- Keep error logs only

### 3. Clean call-ui.tsx
- Remove unused `roomId` destructure
- Keep only essential logs

### 4. Run linter
- `bunx biome check --write .`

### 5. Build and verify
- `bun run build`

---

## Verification

1. Run `bun run build` - should pass
2. Run `bunx biome check .` - should have no errors
3. Test the app - translation and TTS should work with minimal console noise
